@model IEnumerable<Expanse_Manager.Models.total_limit>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Listlimit</title>
    <link href="~/css/dashboard.css" rel="stylesheet" />

</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.total_limit1)
                }
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.total_limit1)
            </td>
           @* <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.total_id }) |
                @Html.ActionLink("Details", "Details", new { id=item.total_id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.total_id })
            </td>*@
        </tr>
    }
    
    </table>
</body>
</html>
