@model Expanse_Manager.Models.total_limit

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>getlimit</title>
</head>
<body>
    <div>
        <h4>total_limit</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.total_limit1)
            </dt>
    
            <dd>
                @Html.DisplayFor(model => model.total_limit1)
            </dd>
    
        </dl>
    </div>
    <p>
        @Html.ActionLink("Edit", "Edit", new { id = Model.total_id }) |
        @Html.ActionLink("Back to List", "Index")
    </p>
</body>
</html>
